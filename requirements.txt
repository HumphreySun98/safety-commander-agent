openai>=1.40
python-dotenv>=1.0
flask>=3.0
Pillow>=10.0
requests>=2.31
scikit-learn>=1.3      # RAG retrieval (TF-IDF) over knowledge/

# Optional — only needed by extract_frames.py to pull frames from a demo video.
# imageio-ffmpeg bundles its own ffmpeg binary (no system install needed).
imageio>=2.30
imageio-ffmpeg>=0.4
# (alternatively: opencv-python-headless, or a system ffmpeg on PATH)

# Optional — only for app_launcher.py (run the dashboard as a native desktop window).
# pywebview>=4.0
