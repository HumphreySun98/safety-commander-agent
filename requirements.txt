openai>=1.40
python-dotenv>=1.0
flask>=3.0
Pillow>=10.0
requests>=2.31

# Optional — only needed by extract_frames.py to pull frames from a demo video.
# Either install this, or have ffmpeg on PATH.
# opencv-python-headless>=4.8
