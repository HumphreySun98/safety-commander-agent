# secrets
.env

# python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# generated artifacts
logs/
reports/

# the hackathon brief (large binary)
*.pdf

# large datasets — never commit these
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.rar
Video Dataset for Safe and Unsafe Behaviours/
sop-sample-training-data/
frames_cctv/
data/

# ML model weights / training artifacts (perception layer) — keep out of git.
# NOTE: perception/*.json and frames_annotated/*.jpg ARE committed (the agent reads them).
*.pt
*.onnx
*.engine
weights/
runs/
datasets/
yolo*.pt

# transient video-mode rep frames (regenerated each run)
frames_annotated/_live_*

# os
.DS_Store
